= render partial:'shared/header'
.analysis_index
  .analysis_menu
    .analysis_menu__large-content
      .title
        =link_to("カテゴリーごと","")
      .title
        =link_to("ユーザーごと","")
      .title
        =link_to("ブランドごと","")
    .analysis_menu__middle-content
      .title
        =link_to("出品個数","")
      .title
        =link_to("出品金額","")
      -# .conditions
      -#   .conditions__period
      -#     .conditions__period__start
      -#       ="開始日時"
      -#       =input_tag()
      -#     .conditions__period__end
      -#       ="終了日時"
    %canvas#myChart{:height => "400", :width => "900"}
    .analysis_menu__small-content
    -@select_categories.each do |category|
      .title
        =link_to(category.name,analysis_search_path(category:category),class:'graph',id:category.id)
= render partial:'shared/footer'
